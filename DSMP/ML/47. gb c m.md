Boosting for Classification Maths Formulation

we have studied how gradient boosting is applied in classification.
now we are going to understand why we are doint these steps, where does the function come from.
some questions:
1. why initial value is log odds
2. why calc difference during calculation of residual
3. why output of leaf of a tree is that formula


starting with deciding loss function, - log loss
L = 1/n * sigma[ yiloguihat + (1-yi)log(1-yihat) ]
these loss function is in terms of output probablity, we convert in log odds space
so we make a small change, replace probabilitiers in formual with logg odds
the reason will be answered later

L = 1/n * sigma[ yiloguihat + (1-yi)log(1-yihat) -yi * log(1-yihat ]
solve full deriva and convert to log odds

ques: why this works better woth log odds instead of probabilities?

write loss for a single data point and for all the rows - gemeral formula

# maths behind classification

step 1: minimize loss function
it is a function of yi and log- oddsi
we need to convert it to yi and gamma, [what is gamma? = log odds i]
<img width="612" height="795" alt="image" src="https://github.com/user-attachments/assets/822211c2-f371-4910-be13-57abe7f4f7b7" />









