mark this one of the most imp lecture of ml

line: gradient boosting is applying gradient descent in fucntional space
till the end of lecture , we will be able to understand this line
why gradient boosting work?

1. understanding functional space vs parameter space

data - experience vs salary
starting with a bad model by using avg of salary for each person.
next step is to improve - need to know how much is the error, and 2nd reduce that error
can be done using loss function, eg mean square error

to reduce this error first understand functional space
write loss function in expanded form
L(Y, Yhat) = sigma(y - yhat)^2

this is a funtion of predictions y1hat, y2hat, ... , ynhat
loss vs these n variables; n+1 dimensional relationship

lets draw these on 2d graph, taking loss on y axis and yi on x,
we were able to calculate loss function because we had yihat values
but if initial function changes, yi values changes, gets a new loss function value.
we can draw infinite functions for these
we need that one function, which gives minimum error.
this we can place any function on the graph on x axis
this is known as function space.


linear regression algo is similar
that is a parametric function mean it assumes that function is a line
it needs to find just ,m and b
in that case loss function is taken on y axis and m or b values on x axis 
and that is called parametric space

but in gb, we don't know form of function so we work with functional space
parameter space is a subset of functional space

2. direction of loss minimization

we have a graph Lossmin vs yihat
we need to minimize the loss,
suppose we are at a point on curve, to reach the minimum, calculate slope and move in -ve direction of it
as we have n variables, calculate n slopes
recall the 2(a) step from prev lecture, we are finding the slope
the slope guides us in which direction we should movw

3.. now we need to update the function
slope is Yi - Yhat(xi) [or - fo(xi)]
f1(xi) = f0(xi) - slope
<img width="810" height="537" alt="image" src="https://github.com/user-attachments/assets/e2b2faf7-4826-40f1-92ad-16a76151e68e" />
this massively overfits
so we use someyhing that gives us something equivalnet of that output
hence we use decision tree - weak learner
so f1x = f0x + h1x

4. iterate

differences btw gradient bosting and descent
descent - parameter space
boosting - function space
Mn = M0 - learning rate . slope
fm(x) = fm-1(x) + hm(x) - decision tree

Advantages:
powerful, flexible



