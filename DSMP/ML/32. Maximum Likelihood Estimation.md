Maximum Likelihood Estimation

intro
used in logistic, linear regression and deep learning 
important
help to understand ml models
so frequently asked in interviews like
prob vs likelyhood
explain mle, how it is implemented in logistic regression.
how it is related to ml

this topic is confusiong, too mathematical, hard
need to understand intution overview.
lets start

what is likelihood

examples
1. tossing a coin [fair]
   follows bernaullis distribution
   have two parameters - p and q
   function p(x)

   probability - for an exp if we know its distribution, its parameters, so if we want to find chance of an event, it will be called probability
   likelihood = opposite view of this
   lets say if someone gave a coin, we tossed it 5 times and gets head 5 times
   doubt: what are the chances head coming 5 times, doubt is it fair, plausibility, is likelihood.
   likelihood = (0.5)^5
   likelihood of this is very low
   we have event and finding what is parameter

   parameter -> event/chance ; prob
   event data -> is parameter justified; likelihood

maximum likelihood estimation
(MLE) is a method of estimating the parameters of a
statistical model given some observed data.
1. coin toss
2. height estimation
using these examples - explain mle

for 1 data point, likelihpod is maximum if mean is equal to tht point - for normal distribution

what if we have more than 1 data point
calculate likelihood by changing mean and variance, given same data, 
the value for which this value is max is maxiimum likelihood


the formula of normal distribution have one value input point
lets say all data points are independent
L(neu,sigma | x1 x2 x3 ... xn) = pdf(x1) * pdf(x2) * pdf(x3) ... pdf(xn)
take log of this
write full derivation

to maximize likelihood , we need ro maximize log of likelihood
we can plot graph between likelihood and mean,
we can differentiate log(likelihood) wrt mean to find maximun

we can apply same logic on variance

















