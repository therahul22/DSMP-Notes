logistic regression
lot of questions in interviews,
conmcepts used in deep learning also

it is a classification problem though its name is regression
it is a linear model - related to lines, planes
seems difficult because of its different interpretation
we are studying probablistic interpretation.


problem - given 2 classes, draw line differentiationg 2 classes
In classification problem we have to make sure the +ve points land in the
+ve region and -ve point lands in the -ve region

how do we decide which model is better - by no. of mis classifications

condition
run a loop 
if pt is red and ax1 +by1 + c >0
  misclass +=1
if pt is green and ax1 +by1 + c < 0
  misclass +=1


but if both models have same mis classification then how to0 decide? like if there is slight diff in slope

classification shoouldnt be black and white
instead of just step function: +ve, -ve or 0 and 1, we need continuous function, 

we should label:
points close to line assign low number, farther one gets high
use sigmoid function
<img width="367" height="238" alt="image" src="https://github.com/user-attachments/assets/d804df45-a139-4774-bb85-b802e99658b0" />
we created a sort of gradient, and can say probability about a point

how to decidde best line:
try to find a loss function then minimize it

maximun likelihood
The likelihood function is the product of the
predicted probabilities for the actual class of
each observation
explain using example used in lecture slides
high number - good model
underflow problen can happen
apply log loss
log of number between 0 and 1 is -ve
we take -ve of log, final number is +ve
for log we need to chase minimum
