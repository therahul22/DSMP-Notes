features are colimns in a dataset
during prediction too much features is also not good, after a certain no of columns ,our model starts deviating
- curse of dimensionality
- sparsity: due to emptyness, performance downgrades
- simplicity
reduces comutation complexity
interpredibility

types of fs:

filter based 
book definition: Filter-based feature selection techniques are methods that use statistical measures to score
each feature independently, and then select a subset of features based on these scores. These
methods are called "filter" methods because they essentially filter out the features that do not
meet some criterion
- study each feature statistically - keep or remove column
- study each feature one by one
- 

plannof action
1. remove duplicate features
  def get_duplicate_columns(df):
    
    duplicate_columns = {}
    seen_columns = {}

    for column in df.columns:
        current_column = df[column]

        # Convert column data to bytes
        try:
            current_column_hash = current_column.values.tobytes()
        except AttributeError:
            current_column_hash = current_column.to_string().encode()

        if current_column_hash in seen_columns:
            if seen_columns[current_column_hash] in duplicate_columns:
                duplicate_columns[seen_columns[current_column_hash]].append(column)
            else:
                duplicate_columns[seen_columns[current_column_hash]] = [column]
        else:
            seen_columns[current_column_hash] = column

    return duplicate_columns

2. Variance threshold mehod
   applied on constamnt and quasi constant feature
   define a threshhold
   find feature variance
   drop cloumn having variance less than our threshold
   note: normalize data before applying variance

        Points to Consider
      Ignores Target Variable: Variance Threshold is a univariate method, meaning it
      evaluates each feature independently and doesn't consider the relationship between
      each feature and the target variable. This means it may keep irrelevant features that
      have a high variance but no relationship with the target, or discard potentially useful
      features that have a low variance but a strong relationship with the target.
      1.
      Ignores Feature Interactions: Variance Threshold doesn't account for interactions
      between features. A feature with a low variance may become very informative when
      combined with another feature.
      2.
      Sensitive to Data Scaling: Variance Threshold is sensitive to the scale of the data. If
      features are not on the same scale, the variance will naturally be higher for features
      with larger values. Therefore, it is important to standardize the features before
      applying Variance Threshold.
      3.
      Arbitrary Threshold Value: It's up to the user to define what constitutes a "low"
      variance. The threshold is not always easy to define and the optimal value can vary
      between datasets.

3. correlation
     find pearson corr coeff between xi and y
   or check multi collinearity between xi,s
   
    Disadvantages
    Linearity Assumption: Correlation measures the linear relationship between two
    variables. It does not capture non-linear relationships well. If a relationship is
    nonlinear, the correlation coefficient can be misleading.
    1.
    Doesn't Capture Complex Relationships: Correlation only measures the relationship
    between two variables at a time. It may not capture complex relationships involving
    more than two variables.
    2.
    Threshold Determination: Just like variance threshold, defining what level of
    correlation is considered "high" can be subjective and may vary depending on the
    specific problem or dataset.
    3.
    Sensitive to Outliers: Correlation is sensitive to outliers. A few extreme values can
    significantly skew the correlation coefficient



annova hypothessis test - refer from lecture slides

chi square : for both categorical data (x, y)



wrapper method

embedded

hybrid technique
