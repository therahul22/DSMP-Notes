logistic regression on multi class classification
high chances of inyterview questions
bettere for inbalanced data how? 

example : cgpa, iq vs placement status  (yes, no or opt out)

techniques: ovr or ova and softmax

1. ovr:
cgpa, iq vs placement status
we train no. of logistic regression models equal to no. of classes here 3
do one hot encoding of output columns
now divide data set into 3 sets
we converted this multiclass to binary classification
we can apply normal logistic regression
prediction - send data point to all 3 models, they will send probabilities
then we normalise it

problem: have to train large no of models if classes are high, time consuming

2. softmax or multinomial logistic regression
softmax function
since lr is linear model, it also drrwa lines
it draw line equal to no of classes
each line has 3 parameter beta0 beta1 beta2
so here total 9 parameters

training: apply one hot encoding, divide dataset
here we use a different loss function, goal is to minimize it 
